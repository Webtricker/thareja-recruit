type PropsType = {
  className?: string;
};
const Component = ({}: PropsType) => {
  // ===== Redux states & functios =======

  // ==== states======

  // ====== Handlers ======

  return (
    <div>
      <h1>SOme content</h1>
    </div>
  );
};

export default Component;
